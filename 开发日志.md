# The Boardroom 开发日志（按日期倒序）

> 这里收：测试记录、中间结论、被推翻的假设、版本迭代、踩坑与教训。
> 最终定稿的规则 / 参数才提炼进 CLAUDE.md——本文件负责细，那边负责精。

## 2026-07-12 ｜ Deck 矮视口文字重叠修复（17:5x 更新）

- 用户报 deck 三处文字重叠（slide III 截图）。根因：slide 为 grid 居中 + 内容高于矮视口可用空间 → 溢出穿进固定 footer；rail 悬停标签无背景直接压正文。补 padding 无效（grid 居中忽略 padding 溢出）。
- 修复：`.slide .wrap` 按高度三档 `zoom`（≤800px→.86 / ≤660px→.72 / ≤540px→.6，900 高不受影响）+ footer 加 paper 渐变遮罩 + rail 标签加底色 chip 且 ≤1240px 宽隐藏。
- 验证：1440×900 / 1333×692（用户截图比例）/ 1000×520 三视口 × 9 页 wrap 全部 fits（rect 断言）+ console 0/0；已推送并轮询确认新构建上线。证据图 output/playwright/deck-answer-fixed.png。

## 2026-07-12 ｜ 上线 + 提交链接（17:3x 更新）

- deck VI 页 DEMO SLOT 填入 "View the live demo" CTA（../site/index.html，本地/线上双通）；根 index.html 作官网入口（跳 deck，附 demo 直达）。
- GitHub：gh CLI 官方二进制（免 brew）+ 设备码登录（用户输码）→ 建 `Craboss888/laoban-ai`（public）→ push main（38 文件）→ API 开 Pages（main/root）。
- 线上全验：root/deck/demo 三 URL 200；live demo `#act0!` 即时态、logo/秘书图加载、console 0/0。
- 口径注意：并行会话已把 tagline 全站改为 "AI might agree with you. Your board won't."（原 ChatGPT will agree），提交文案须用新口径。

## 2026-07-12 ｜ Logo 接入 demo（17:0x 更新）

- `项目 logo.png` 平面图形 Vision 抠像无效（no foreground instances）→ 改 Swift 色键脚本（角点取背景色 + 距离软阈值），出透明底后 sips 裁两资产：`site/assets/logo-lockup.png`（640 宽横版）+ `logo-mark.png`（256² 圆标）。
- 落点四处：页头 wordmark（26px lockup 替换文字）、landing hero 圆标（72px 居中）、memo 页头 lockup（24px）、favicon（mark）。印章内 LAOBAN.AI 保留文字。
- 验证：Playwright 重载 landing/#act6，三处图片 naturalWidth 正常、favicon 生效、console 0/0；截图 output/playwright/landing-logo.png。孤儿 `.dot` 规则已随手清理。

## 2026-07-12 ｜ Pitch deck（laoban.ai 品牌 + demo 色板）（17:0x 更新）

- 交付 `docs/pitch-deck.html`：纯静态单文件 pitch deck（系统字体、零网络、零依赖），9 页议程制结构（会议纪要隐喻：I. Call to Order → IX. Adjournment，左侧罗马数字议程栏）。含问题、方案、机制（交互式三阶段图）、Charter 六条、Decision Memo 解剖、Sovereignty 对齐、商业化（OPC + 订阅 $29/$79 + 高危单次 $99 + 董事市场路线）。demo link 留位（VI 页 HTML 注释 DEMO SLOT）。
- **品牌**：logo `项目 logo.png` 裁剪为 `docs/assets/laoban-logo.png`（sips 居中裁 1000×360），页头/封面/收尾三处嵌入，`mix-blend-mode:multiply` 让奶油底融进页面。
- **色调重定**：初版深色胡桃木+黄铜方向被用户否决 → 全量换为 demo（site/index.html）色板：paper #FAF8F5（对齐 logo 底色）/ ink / baize 墨绿 / seal 印章红，立场语义色 support/oppose/conditional 与 demo 一致；圆角 14px + demo 同款阴影。字体：系统 sans 800 标题 + 衬线斜体作"文件声"点缀 + mono 台账标签。
- **数字口径与缓存对齐**：盲评支持分 78/70/35/15/20 → 质证后 80/72/62/32/28（取自 site/cache.js），kill criteria 文案同源（6 周流失 >5% 或转化 −30% 自动作废）。中途发现 demo 已从 P(success) 小数改为 support score 百分比，deck 全量跟进。
- 交互：键盘/滚轮/触摸/议程栏四路导航 + hash 深链；IV 页三阶段自动播放（3.2s/6.9s，点击接管）+ 支持分数字 tween；VI 页印章弹落动画；prefers-reduced-motion 全局降级。
- 验证：9 页逐页截图（1440×900）、phase 1/2/3 点击与自动播放实测、console 0 errors、内联 JS `new Function` 语法检查、slides/rail 结构一致性自检。本地验证用 `.claude/dev-server-root.mjs`（:8931，服务仓库根目录，非交付物）。
- 踩坑 ×4：①**hash-only 导航不重载页面**——编辑后仅改 hash 验证会跑旧 CSS/JS，须 `location.reload()`（与 e2e README 里 Playwright 的同类坑同根因）；②浏览器对 `#slug` 锚点做原生滚动，overflow:hidden 也会被顶偏 21.5px → go()/init/scroll 三处归位兜底；③`.msec .sk` 选择器漏掉 ruling 区的 `.sk`（改为 `.memo-paper .sk`）；④自动化里 reload 后首次按键无焦点、点击一次页面即恢复——真实使用无此问题。
- 遗留：deck 引用相对路径 `assets/laoban-logo.png`，移动 pitch-deck.html 时须连同 `docs/assets/` 一起；demo 打磨完成后把链接/嵌入补进 VI 页 DEMO SLOT。

## 2026-07-12 ｜ 改名 laoban.ai（16:5x 更新）

- 用户定名 laoban.ai（原 The Boardroom，内核不变）。demo 全量改名：index.html 7 处（title/wordmark/eyebrow/memo×3/seal）+ cache meta + README + submission + generate.py；`window.BOARDROOM` JS 内部标识符刻意保留（不可见，避免连锁改动）。spec/开发日志留历史名。
- 验证：残留 grep 仅历史文档；4 校验器 + node --check + py_compile 过；Playwright 重载 landing/#act6 实测新品牌串，console 0/0；证据图 output/playwright/memo-laoban.png。

## 2026-07-12 ｜ Act 0 三场景重设 + 秘书形象（16:3x 更新）

- 用户要求把 demo 开场整个重设为三场景叙事：①公司介绍（"Imagine you run an AI newsletter company." + 桌上四份 mock 数据卡）→ ②业务压力（口语化担忧大字 + finance/readers/terms 三条互怼信号 + "you are the only leader" 点题）→ ③秘书出场（抠像形象丝滑入场 + 打字机自动预填口语问题的可编辑输入框，点 "Ask the Board Secretary" 后正式决策卡展开）→ 进 Assemble。
- 结构决策（用户选定）：三场景收在 act0 内部推进（复用 act4 回合模式），stepper 七步与 `#act6` 等深链全部不变；交互选打字机预填 + 点击触发。
- 秘书形象：`秘书形象.png`（1254²）用 macOS Vision `VNGenerateForegroundInstanceMaskRequest` 本地抠像（Swift 脚本，零依赖），主体边缘干净、自带奶油描边，缩至 700² 存 `site/assets/secretary.png`（292KB）。
- 技术落点：`ActScripts.act0` → `a0go/a0reset/enterSecretaryScene/a0ask/a0formalize`；预填读现成 `meta.question_raw`，正式卡为静态 HTML（缓存零改动）；`#act0!` 直达场景 3 完成态；进度点 3 枚示位；清掉 company-card/news-strip/price-question 等孤儿 CSS。
- 验证：TDD——secretary-formalization 合同先改先红（`a0s1` 缺失）后绿；4 校验器 + node --check + diff check 全过；Playwright 真浏览器全流程实测（场景推进、打字机 85 字符预填、Ask 后正式卡 + Assemble CTA、进 `#act1`），`#act0!`/`#act4!`/`#act6` 冷加载全过，全会话 console 0 errors/0 warnings；截图存 `output/playwright/act0-scene3-*.png`。
- 踩坑 ×2：①内嵌浏览器面板 timer/rAF 节流导致打字机慢放、过渡卡中间态——已知怪癖复发，真浏览器无此问题，验证一律走 Playwright；②**Playwright `page.goto` 对仅 hash 变化的 URL 不重载页面**，boot 只读一次 hash → 深链验证得到的是上一状态的假阳性（`#act0!` 首次"通过"实为手动走查残留）；改用 `location.hash + location.reload()` 强制重载后才是真验证。已记入 e2e README。

## 2026-07-12 ｜ Secretary 决策结构化（15:4x 更新）

- 用户补充秘书职责：应将 Founder 的口语化困惑转为结构化的正式决策问题。
- Act 0 问题卡现在有两层：`Founder’s question` 呈现“$29 已不够、又担心伤害读者信任”的口语担忧；`Board Secretary · Formal decision` 将其整理为“是否将月订阅从 $29 调至 $49”，并显式记录范围（新订阅者 + 现有读者承诺）和两周截止时间。
- 正式问题同步到圆桌中央与 Decision Memo，且 memo 上下文改为 AI news subscription，去除旧的 subscriber/MRR 上下文。
- 验证：先写 formalization contract 并确认旧 UI 失败；实现后 Act 0 的 1273×779 实际截图完整可读，Memo 直达亦显示同一正式问题；浏览器 console 0 errors，三份 cache/UI contract、JS syntax 与 diff check 均通过。

## 2026-07-12 ｜ 支持度百分比（15:3x 更新）

- 用户要求所有董事数值改成直观的“对当前方案的支持度”：100% = 完全支持，0% = 完全不支持；不再呈现为净收益概率。
- 统一新增 `supportPercent()` 显示格式：Blind Review、Cross-Examination 和 Decision Memo 都从 `0.80` 等内部值显示为 `80%`。Memo 标题改为 `DIRECTOR POSITIONS & SUPPORT SCORES`，列说明改为 `SUPPORT FOR THIS DECISION`；预设发言和 memo 摘要中残留的小数也转为百分比。
- 内部字段为兼容缓存仍叫 `prob`，但其语义与生成器 prompt 已改为 support score；分歧仪表盘继续对这些支持度作标准差计算，数值机制未改。
- 验证：新建 support-score contract；Playwright 分别复验 Blind Review 与 Memo，78% / 80% / 72% / 62% / 32% / 28% 均正确出现，console 0 errors。cache contract、cache validator、JS syntax、diff check 均通过。

## 2026-07-12 ｜ 常驻 Founder Channel（15:2x 更新）

- 用户纠正交互：Founder input 不是在第一回合后才“出现”，而是整个 Act 4 固定存在，到预设节点才自动填入 demo 文本。
- 调整为桌面端右侧常驻 Founder channel：Round 1 期间显示 `Waiting for Round 1` 的禁用空输入；Round 1 完成后自动预填、解锁并高亮；发送后输入框仍保留，切换为 `Founder message recorded`，董事继续回应。窄屏改为单列，避免压缩讨论正文。
- 验证：先加静态 contract 断言，确认旧实现失败（composer 在 Round 1 之后才创建）；改后 cache contract、cache validator、JS 语法和 diff 检查均通过。Playwright 在 1273×779 实测常驻输入、自动预填、发送后保留状态和后续回应；console 0 errors/0 warnings。

## 2026-07-12 ｜ Founder 多回合质证（15:1x 更新）

- 用户要求墙落后的董事会呈现为真实感更强的多回合会议，并允许 Founder 介入；采用**完全预置的稳定 demo 回放**，不接入实时模型。
- Act 4 的 14 条既有质证记录重组为三个回合：证据对齐 → 回应 Founder → 记录残余异议。第一回合结束后出现可编辑的英文 Founder input，默认预填一条询问“合规、可逆的 $49 调整”的发言；发送后，该发言先写入记录，再触发预设的第二、三回合。
- 技术落点：缓存新增 `act3_discussion`（Founder prompt + 三段事件索引），前端以 round runner 播放；Customer Representative 的条件支持、CLO 与 Mr. Opposite 的残余异议都保留在后两回合，最终才显现 Memo CTA。`#act4!` 可瞬间直达 Founder 输入区，方便现场/验收。
- 验证：先写 cache contract 并确认 `KeyError: 'act3_discussion'`，再补齐实现后 `FOUNDER DISCUSSION CACHE OK` 与 `CACHE OK: 14 debate events`。Playwright 实测输入可编辑、Founder 文本进入记录、Round 2/3 和 Memo CTA 全部出现，点击后到达 `#act6` Decision Memo；浏览器 console 0 errors/0 warnings。
- 踩坑：会议底部的固定导航遮住了 Founder send button（1273×779 CSS viewport 截图复现），已在 Act 4 隐藏该导航；截图复验后发送按钮完整可用，键盘左右键仍可用于测试导航。

## 2026-07-12 ｜ Evidence Routing（14:5x 更新）

- 用户确认：Assemble 之后、Blind Review 之前需加入独立的数据分发步骤。流程由六幕扩为七幕：Context → Assemble → **Route** → Blind → Examine → Adjourn → Memo。
- 新 Act 2 将四条证据明确分配为 `finance.csv → CFO`、`market.md → CMO`、`reviews.json → Customer Representative`、`terms.md → CLO`；Mr. Opposite 标明为无业务数据、仅领取挑战创始人核心假设的授权。四条带角色色彩的连线在进入盲评前依次显示。
- 状态机、深链和文档入口随之顺延：Memo 直达改为 `#act6`，完成态 Blind 改为 `#act3!`。
- 验证：首次 Playwright 走查捕获到新增 `act2` 函数遗漏 `async`，导致浏览器报 `await is only valid in async functions`；按根因补齐后，实际点击 Route CTA 已进入 Blind，Blind→Examine 和 `#act6` Memo 直达均通过，三次浏览器控制台均为 0 errors/0 warnings。`validate_cache.py`、内联 JS `node --check`、`git diff --check` 均通过。

## 2026-07-12 ｜ 英文化接力（13:xx 更新）

- 用户明确要求最终提交物全部使用英文。已完成提交面英文化：六幕 UI、场景缓存、mock 数据、编排器 prompt/注释、README、submission copy、测试说明。
- 英文术语锁定：Blind Review / Evidence Reveal / Cross-Examination / Dissent Log / Kill Criteria / Founder's Ruling；tagline 为 “ChatGPT will agree with you. Your board won't.”
- 顺手消除一处 schema 口径偏差：`generate.py` 的 `act2_blind` 输出改为与缓存一致的数组形态。
- 自检结果：数据 JSON 解析通过；`CACHE OK: 12 debate events`；`py_compile`、`--dry-run`、内联 JS 语法、`git diff --check` 均通过；提交面中文残留扫描为 0。
- 浏览器复验被本地页面安全策略拦截，未绕过。由于仅改文案未改状态机，下一步必须在正常前台浏览器完成完整英文 E2E，并重点检查英文长文案换行，再生成提交截图。

## 2026-07-12 ｜ 董事会重构对齐（14:20 更新）

- 外部会话（用户另用一个 AI）完成：全量英文化（提交物要求）+ act0 董事会构建器（固定 4 席 + 2 自适应席 + demo guard）。
- 本会话完成 **T9 全链路对齐**：审议阵容从旧 4 人组改为新 5 人组——CLO 接管条款管道（法务论证），CMO 吸收行业情报，客户代表更名，**Mr. Opposite 变纯授权反对者**（无数据管道，盲评攻击锚定与不可逆性，质证中逼出否决线数字，双异议入档）。盲评分歧 0.26 → 质证后 0.21（收窄不归零，数字自洽）。
- 验证：validate_cache（含 board 块一致性断言）✓；node --check ✓；内嵌浏览器 act0/2/3/5 全态 ✓；**playwright 第二引擎全程走查 0 errors/0 warnings** ✓。
- 截图集入库 output/screenshots/（landing / act0 / act2 / act3 / memo-hero，hero 已隐藏悬浮控件重截）。
- 踩坑：dev server 查询串处理 bug（`/?v=2` → 404），验证时绕过；hash-only 导航不触发重载（boot 只读一次 hash），改用 JS 驱动验证。

## 2026-07-12 ｜ 开发实况（13:0x 更新）

- 设计定稿要点：LangGraph 弃用（用户不熟）→ 手写 Python 编排；评委指引「demo version + mock data + 重 UI/UX」→ 范围收窄到单场景六幕；「会议室围坐」保留但反谄媚病根定位在**架构**（同脑同上下文）而非画面——视觉重点改为把「信息隔离」演出来（信息舱墙起→墙落）。
- 进度：T1 数据包 ✓（12 个月流水逐月自洽校验）/ T2 缓存 ✓（CACHE OK: 12 debate events）/ T3 壳+Memo 屏 ✓（印章裁决交互）/ T6 编排器 ✓（py_compile + dry-run）/ T7 README+submission ✓（禁词自查 PASS）/ T4 幕 0–2 已验证，幕 3–4 待浏览器恢复后验证。
- **踩坑记录**：
  1. macOS 系统 python3 起 http.server 被 TCC 拦（Desktop 权限）→ 换 node 起 dev server（.claude/dev-server.mjs，不入库）。
  2. 内嵌浏览器面板 `visibilityState=hidden` → rAF 节流：流式只在截图强制渲染时前进、一次性 rAF 挂 class 不可靠 → 加 reveal()（rAF+setTimeout 双触发，幂等）。**真实前台浏览器无此问题**，e2e README 已记录。
  3. 浏览器面板程序化滚动后截图偶发空白（合成器怪癖）→ 验证时改用整页高视口 + JS 驱动。
  4. `file://` 无法在内嵌面板打开（仅 http）→ 交付物仍是 file:// 直开，验证走本地 :8899。
- 新增小功能：URL hash 深链 `#act5` / `#act2!`（快进）——测试通道 + 评委直达 + 现场救场三用。

## 2026-07-12 ｜ 项目启动（preflight）

**环境配置结果**（cc-preflight 验证清单摘要）：

- ✅ **记忆文件**：CLAUDE.md（含协作铁律 + 项目信息，项目名/目标/验收标准从 spec 提取）、本开发日志已生成。
- ✅ **目录结构**：`data/ docs/ output/ tests/ references/ scripts/` 已建（源码目录留到搭骨架时定）。
- ✅ **装备五项全部就位**：
  - playwright（plugin，浏览器自动化）——已启用，`mcp__plugin_playwright_*` 工具可用
  - frontend-design（plugin，前端设计）——已启用，skill 列表可见
  - superpowers（开发方法论 skills 集）——已启用，brainstorming/TDD 等子 skill 可见
  - agent-reach（CLI，全网调研）——`/Users/xf/.local/bin/agent-reach` 已装，`doctor` 退出 0；各平台渠道为低风险基础配置（按清单本就不启用 --channels=all / 不抽 cookies）
  - web-design-engineer（skill，网页设计工作流）——`~/.claude/skills/web-design-engineer/` 已装
- ✅ **每轮自检 stop hook**：`~/.claude/settings.json` 已配置（指向 cc-preflight/hook/每轮自检hook.sh），先前会话已完成，本次沿用未改动。
- 备注：cc-preflight skill 本身已安装于 `~/.claude/skills/cc-preflight`，与上游最新版逐文件一致。

**初始决策**：

- 参赛项目锁定为 The Boardroom（spec 已定稿于 boardroom_hackathon_spec.md）。
- 开发路径遵循 spec 第 7 节 MoSCoW：先端到端跑通 Must 骨架（LangGraph 图 + 数据三件套 + Memo 渲染 + 缓存回放 + 提交材料），11:00–15:30 目标窗口。
- 尚未动工任何业务代码——等待开工令。
