# 测试:六幕端到端走查

- **验证什么**:`site/index.html` 从 landing 依次走完六幕(组建→议题→盲评→质证→休会→备忘录),外加「跳到结尾」与深链(`#act5`、`#act2!` 快进态)。
- **通过标准**:
  1. 全程 console 零 error;
  2. 幕 2:四信息舱并行流式、完成后立场卡齐(0.78/0.35/0.15/0.70),分歧仪表 0.26 红区,「墙落」CTA 出现;
  3. 幕 3:两大节拍高亮可见(客户董事「立场已修正」/ Mr. Opposite「异议幸存」),仪表随质证收窄至 ~0.21,CEO 休会按钮出现;
  4. 幕 5:备忘录四要素 3 秒可辨(异议记录/概率/否决线/裁决),点裁决出现红章,「撤销重签」可复位;
  5. 键盘 ←/→ 可切幕;窄屏 900px 不破版。
- **运行方式**:本地打开 `site/index.html`(或 `node .claude/dev-server.mjs` 后访问 :8899),按上述清单人工+截图走查;截图存 `output/screenshots/`。
- **已知环境限制**:嵌入式浏览器面板将页面判为 hidden 时 rAF 节流,流式动画需前台标签页观看——真实演示环境(前台浏览器)不受影响;过渡类挂载已用 reveal()(rAF+定时器双触发)兜底。
