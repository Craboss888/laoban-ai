# Founder Multi-Round Discussion Contract

## Validates

The cached post-reveal transcript contains exactly three deterministic discussion rounds and one non-empty preloaded Founder prompt.

## Pass condition

`python3 validate.py` exits 0 and prints `FOUNDER DISCUSSION CACHE OK`.

## Run

```bash
python3 tests/founder-discussion/validate.py
```
